hello neeraj bhaiya today i have learned about the HELM so to understand the helm ,it help to start with the problem it solves :
deploying an applicaion in kubenetes we usually requires to write and manage mutiple YAML configuration files like one for the depolyment container one for the network service one for the storage and the secret and so on so heres the HELM comes into the picture HELM is a package manager for kubernetes it acts like APT for ubuntu npm for node.js and brew for macOS and yum for red hat so it allows us to bundle all these seprate Configuration files into a single resuable package thatb you can install using in one command ..next Topic is HELM chart so itis a chart that contains all the pre configured kubernetes resource template that is needed to run a application.. nect is _helper.tpl and the Is CRD 
